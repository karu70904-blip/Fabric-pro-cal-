{
  "name": "fabric-pro-calculator",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "build": "mkdir -p www && (cp index.html www/ || cp Index.html www/)",
    "ionic:build": "npm run build",
    "postinstall": "npx cap add android || true"
  },
  "dependencies": {
    "@capacitor/android": "^5.0.0",
    "@capacitor/core": "^5.0.0"
  },
  "devDependencies": {
    "@capacitor/cli": "^5.0.0"
  }
}